# 雅砻江运维 Demo

## 0902-融合新增功能（AI工作台布局）

- 将 V6 的 AI 工作台右侧布局与交互迁入 0901 高保真视觉，覆盖 DF-20260820-001/002/003。
- 同步 Smart Assistant 的 LUI/GUI、待办/已处理和新建入口。
- 以「数据收集 → 根因分析 → 诊断结论」替换旧根因排行，保留原文案、图表数据及 A/B/C/D 处理选项。
- 保留明暗主题，默认深色；源码和 `dist/` 构建产物一同保存。
- 仓库沿用根目录应用结构；对应本地交付目录的 `app/`。双击根目录「启动雅砻江运维.command」即可启动。
- Mapbox 令牌不随公开仓库发布。需要在线地图时，将 `.env.example` 复制为 `.env.local`，配置 `VITE_MAPBOX_ACCESS_TOKEN`；静态构建需重新运行 `npm run build`。

## 本次更新说明

- 全局组件化、地图增加电站 3D 模型、思考渐变动画优化。
- 未改默认相机视角。
- 未增加离线地图。
- 增加浅色适配。
- 更改布局与流程。
